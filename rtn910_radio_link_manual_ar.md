# RTN 910: كتابة أوامر Radio Link يدويًا بصورة موثقة

> **نطاق هذا الدليل:** الأوامر أدناه مستخرجة من مقال Huawei الرسمي **EKB0000559092** لحالة محددة اسمها RTN910V1R1 استخدمت أداة Navigator عندما لم تعمل WebLCT/U2000. ليست أوامر عامة لكل إصدارات RTN 910، ولا تعد بديلًا عن مخطط التردد، التصريح التنظيمي، أو دليل الإصدار المطابق للجهاز. Huawei نفسها توصي في المقال بعدم جعل Navigator مسار التهيئة العادي.[1]

## أولًا: ما الذي يمكن تأكيده وما الذي لا يمكن تأكيده

تؤكد Huawei أن مسار Navigator في المقال يغطي فحص اللوحات الفعلية والمنطقية، إضافة لوحة، ضبط تردد إرسال الـ ODU، ضبط قدرة الإرسال، ضبط IF channel bandwidth، وقراءة Link ID.[1] أما دليل Configuration Guide المتاح لإصدار مختلف **V100R003C03** فيوثق نفس منطق التهيئة عبر U2000: إدخال معلومات IF/ODU، ضبط ATPC عند الحاجة، ضبط سمات تردد الـ ODU والقدرة، ثم إنشاء/اكتشاف وصلة الراديو في الطوبولوجيا.[3]

لا يثبت المقال العام **وحدة** قيمة `7177000` أو ترميز قيمة bandwidth `4` أو تفاصيل معاملات أمر modulation؛ إذ يشير المقال إلى ملف مرفق لشرح معاملات الأوامر ولم يكن ذلك المرفق متاحًا للاستخراج. لذلك لا يجوز تحويل هذه القيم المنشورة إلى أرقام تُعاد استخدامها في موقع آخر.

## متطلبات قبل كتابة أي أمر

| شرط | سبب عملي |
|---|---|
| رقم الإصدار الفعلي للجهاز ونسخة Navigator/WebLCT | مقال Huawei يتعلق بـ RTN910V1R1، بينما مسارات RTN 910 الأخرى تختلف. |
| Link Engineering Sheet للطرفين | مصدر تردد الإرسال، T/R spacing، عرض القناة، الاستقطاب، والقدرة؛ لا تستنتج من مثال ويب. |
| جرد اللوحات والـ ODU الفعلي | معرف اللوحة والمنفذ والـ ODU في الأمر يتغيران حسب العتاد المركب. |
| موافقة تغيير ونافذة صيانة إن كان الرابط قائمًا | تردد/قدرة/عرض قناة غير متوافق في طرف واحد يمكن أن يقطع الوصلة أو يسبب تداخلًا. |
| نسخة احتياطية وسجل قيم البداية | لإعادة القيم السابقة إذا فشل التحقق بعد التغيير. |

## تسلسل Navigator الموثق من Huawei لحالة V1R1

نفّذ أوامر القراءة أولًا، وسجل ناتجها قبل أي تغيير. الصيغة تبدأ بعلامة النقطتين، والأمثلة المنشورة تستخدم فاصلة للفصل وفاصلة منقوطة لإنهاء أمر الضبط.[1]

### 1. فحص الجرد قبل التغيير

```text
:cfg-get-phybd
:cfg-get-board
```

الأول يعرض اللوحات الفعلية، والثاني يعرض اللوحات المنطقية المعترف بها. لا تنتقل إلى إضافة لوحة أو ضبط ODU إذا لم تطابق نتائج الأمرين الـ BOM والجرد الفعلي.[1]

### 2. إضافة لوحة — فقط إذا كانت موجودة فعليًا ولم تتعرف عليها المنصة

المقال يعرض المثال التالي:

```text
:cfg-add-board:24,tnd1odu
```

هذا **ليس قالبًا عامًا**. الرقم `24` ونوع اللوحة `tnd1odu` خاصان بحالة المقال. اكتب هذا الأمر فقط بعد أن يثبت `cfg-get-phybd` موضع اللوحة الفعلي، وبعد مطابقة نوع اللوحة من دليل جهازك.[1]

### 3. قراءة إعدادات الـ ODU الحالية

المثال الرسمي لقراءة تردد الإرسال هو:

```text
:radio-cfg-get-odu-txfreq:23,0xFF
```

استبدل معرف ODU في المثال بالمعرف الذي أظهره الجرد الفعلي. لا تضبط التردد قبل توثيق قيمة الطرفين، ووضع Tx high أو Tx low، والمسافة T/R المعتمدة.

### 4. ضبط تردد الإرسال — لا تستخدم قيمة مثال Huawei

المقال الرسمي يعرض هذا المثال:

```text
:radio-cfg-set-odu-txfreq:23,0xFF,7177000;
```

استخدمه **بوصفه شكلًا للأمر فقط**. لا تستخدم `7177000` في موقعك. دليل U2000 لإصدار V100R003C03 يوضح أن تردد الإرسال وT/R spacing يجب أن يطابقا خطة الشبكة، وأن فرق تردد الإرسال بين طرفي الوصلة يكون بمقدار T/R spacing، وأن T/R spacing نفسه يجب أن يتطابق في الطرفين.[3]

### 5. ضبط قدرة الإرسال — لا تفسر القيمة المنشورة وحدك

المقال يعرض:

```text
:radio-cfg-set-odu-tsl:23,0xFF,200;
```

لا تنسخ `200`. وحدة/ترميز هذا المعامل يجب أن تؤخذ من attachment/command-parameter reference المطابق لإصدارك. في دليل U2000، يجب أن تكون Maximum Transmit Power وTransmit Power ضمن خطة الشبكة والحد الاسمي للـ ODU، وألا تتجاوز القدرة التي يمكن أن تضبطها ATPC الحد الأقصى المحدد.[3]

### 6. قراءة وضبط IF channel bandwidth

المقال يوثق القراءة التالية:

```text
:radio-cfg-get-if-bandwidth:3,0xFF,0
```

ويوثق مثال ضبط العرض التالي:

```text
:radio-cfg-set-if-bandwidth:3,0xFF,1,4;
```

المعرفات `3` و`1` والرمز `4` ليست قيمًا عالمية. اعتمد فقط قيمة عرض القناة المطابقة لـ Link Engineering Sheet، وللوحة IF والـ ODU المركبين. لا تغيّر عرض القناة في طرف واحد دون خطة تغيير متزامنة للطرف المقابل.[1] [3]

### 7. قراءة Link ID ثم التحقق من التعديل بحذر

المقال يوثق قراءة Link ID بهذا الشكل:

```text
:radio-cfg-get-linkid:3,0xFF,1
```

كما يسرد الأمر التالي عند الحديث عن نوع modulation:

```text
:radio-cfg-set-if-currmod
```

لكن المقال يصفه بأنه للتحقق من modulation في حين أن اسم الأمر يبدأ بـ `set` ولا يوضح أي معاملات. **لا تنفذه ولا تخترع معاملاته**. استخدم شاشة U2000 أو مرجع معاملات Navigator المرفق بالمقال لنفس الإصدار. يوضح دليل U2000 أن تغيير خصائص Hybrid/AM يجب أن يبقى متطابقًا في طرفي الوصلة، وإلا قد لا يسري التعديل أو قد تنقطع الخدمات.[3]

## خطوة إنشاء/اكتشاف الوصلة بعد ضبط الطرفين

إذا كانت U2000 متاحة، فإن دليل V100R003C03 يوثق التسلسل التالي لإنشاء الوصلة عبر البحث بدل اختراع معرفات Link يدويًا:

1. من القائمة الرئيسية اختر `File > Discovery > Fiber`.
2. من `Subject Tree` اختر لوحة IF التي تريد البحث عن وصلة الراديو عليها.
3. اضغط `Search`.
4. بعد نجاح البحث، اختر الوصلة من `Physical Fiber/Cable Link List` ثم اضغط `Create Fiber/Cable`.
5. إذا ظهرت وصلة متعارضة في `Logical Fiber/Cable Link List`، افحصها واحذفها فقط إذا كانت غير صحيحة أو لم تعد مستخدمة، ثم أنشئ الوصلة الصحيحة.[3]

## التحقق بعد التهيئة

بعد كل تغيير، أعد أوامر القراءة نفسها وسجّل النتيجة. ثم تحقق من الجرد، وتردد الإرسال، وعرض القناة، وLink ID في الطرفين. عند استخدام U2000، تحقق من معلومات الـ ODU عبر `Configuration > ODU Interface > Equipment Information > Query`. وتفيد شاشة `Configuration > ATPC Adjustment Records > Query` في مراجعة سجلات عمل ATPC عندما تكون الوظيفة مستخدمة.[3]

لا تعتبر الوصلة جاهزة للخدمة بمجرد قبول الأمر. تطابق تردد/spacing، حالة ODU، RSL/الجودة، alarms، والخدمة الطرفية يجب أن يُثبت مقابل خطة القبول الخاصة بالموقع.

## الحد الفاصل الصادق

إذا كان جهازك ليس **RTN910V1R1** أو لم تكن تملك مرجع معاملات Navigator، فالأوامر المنشورة أعلاه تصلح كدليل بحث ومراجعة فقط. لإكمالها بصورة دقيقة، أرسل ناتج `cfg-get-phybd` و`cfg-get-board` بعد إخفاء المعرفات الحساسة، وصورة الإصدار، وLink Engineering Sheet. عندها يمكن استبدال الأمثلة بمتغيرات موقعك دون اختراع قيمة واحدة.

## المراجع

[1]: https://support.huawei.com/enterprise/en/knowledge/EKB0000559092 Huawei Support, “FAQ - How to Configure Basics Paramenters for the Microwave Link by Navigator,” published 2012-07-25.

[2]: https://support.huawei.com/enterprise/en/enterprise-network-microwave/optix-rtn-910-pid-60977 Huawei Support, “OptiX RTN 910 Support Guide, Manuals & PDF.”

[3]: https://pdfcoffee.com/optix-rtn-910-configuration-guidev100r003-pdf-free.html “OptiX RTN 910 Configuration Guide (U2000), V100R003C03, Issue 04.” Secondary hosted copy of a Huawei document; it is used only to corroborate the U2000 workflow and must be matched to the installed release.

## حالة المصادر الرسمية وحدود المطابقة

تم التحقق من صفحة دعم Huawei الخاصة بـ OptiX RTN 910 ومن مقال المعرفة الرسمي EKB0000559092. لم تُستخرج من بوابة الدعم العامة نسخة Configuration Guide أو Commissioning Guide مطابقة لإصدار RTN910V1R1؛ لذلك لا يُقدَّم المرجع الثانوي V100R003C03 على أنه دليل الإصدار الحالي، بل كقرينة على مسار U2000 فقط. أي معاملة غير مذكورة صراحة في المقال الرسمي، ومنها معاملات `radio-cfg-set-if-currmod`، تبقى غير معتمدة ولا تُخمن.

## خطة rollback المقيّدة بالإصدار

1. قبل أي تغيير، احفظ ناتج `cfg-get-phybd` و`cfg-get-board` وأوامر القراءة الخاصة بـ ODU وIF وLink ID، وسجّل قيم البداية في ورقة التغيير.
2. نفّذ تغييرًا واحدًا فقط في نافذة صيانة معتمدة، ولا تغيّر طرفي الوصلة في وقت واحد. لا تستخدم قيمة المثال المنشورة بدل القيمة الموجودة في ورقة Link Engineering Sheet.
3. أعد أوامر القراءة بعد التغيير، ثم افحص تطابق الطرفين، حالة ODU، alarms، RSL/الجودة، والخدمة. إذا فشل أي تحقق، أوقف باقي التغييرات.
4. الاسترجاع يعني إعادة **القيمة السابقة الموثقة فقط** باستخدام صيغة الأمر التي يثبتها مرجع معاملات الإصدار نفسه. إذا لم يثبت المرجع صيغة المعامل أو وحدته، لا تنفذ rollback تخمينيًا؛ اعزل الخدمة وفق خطة الموقع وصعّد الحالة إلى فريق Huawei/مالك الشبكة.
5. بعد الاسترجاع، أعد جميع أوامر القراءة وسجّل نتيجة post-check، ولا تعلن عودة الوصلة إلا بعد قبول الطرفين والخدمة وفق سجل القبول.

هذا القسم يصف منهج تحكم بالمخاطر، وليس تصريحًا بتنفيذ أوامر على جهاز حقيقي دون مرجع الإصدار وخطة التغيير.
